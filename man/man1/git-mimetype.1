.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.mimetype(1)" "" "" "" ""
.hy
.SH NAME
.PP
git mimetype
.SH SYNOPSIS
.PP
git mimetype - execute unix file \[en]mime command on all tracked files
.SH DESCRIPTION
.PP
git mimetype is a helper alias to execute the unix file \[en]mime
command on all tracked files.
.SH EXAMPLES
.IP \[bu] 2
\f[C]git mimetype\f[R]
.IP \[bu] 2
\f[C]git mimetype ascii\f[R]
.SH SEE ALSO
.PP
filetype notasciilist
.SH DETAILS
.IP
.nf
\f[C]
mimetype = \[dq]!f() { : git man mimetype; \[rs]
    command() { git ls-files -z | xargs -0 file --mime; }; : Base command, file --mime, to run; \[rs]
    if [ $# -gt 0 ]; then command | grep \[rs]\[dq]$\[at]\[rs]\[dq];           : If arguments, filter through grep; \[rs]
    else command; fi; :                                    : If not, just output; \[rs]
}; f\[dq]
\f[R]
.fi
