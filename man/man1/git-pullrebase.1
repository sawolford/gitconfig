.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.pullrebase(1) User manual" "" "April, 2020" "" ""
.hy
.SH NAME
.PP
git pullrebase
.SH SYNOPSIS
.PP
git pullrebase - fetch, then rebase HEAD with its upstream branch
.SH DESCRIPTION
.PP
git pullrebase is a helper alias to rebase HEAD with its upstreaem.
.IP "1." 3
Ensures that the current HEAD is not a detached HEAD
.IP "2." 3
Ensures that the current branch is associated with an upstream branch
.IP "3." 3
Downloads changesets from the origin remote
.IP "4." 3
Stores the upstream branch name associated with HEAD in a variable
.IP "5." 3
Calls the rebaseto with a target of the upstream branch associated with
the current HEAD
.SH EXAMPLES
.IP \[bu] 2
git pullrebase
.SH SEE ALSO
.PP
rebaseto
.SH DETAILS
.IP
.nf
\f[C]
pullrebase = \[dq]!f() { : git man pullrebase; \[rs]
    if ! git symbolic-ref HEAD; then echo \[aq]on detached head\[aq]; return 1; fi; \[rs]
    if ! git rev-parse --abbrev-ref \[at]{u}; then echo \[aq]no upstream branch\[aq]; return 1; fi; \[rs]
    git fetch; \[rs]
    UPSTREAM=$(git rev-parse --abbrev-ref \[at]{u}); \[rs]
    git rebaseto $UPSTREAM; \[rs]
}; f\[dq]
\f[R]
.fi
.SH AUTHORS
Scott Wolford.
