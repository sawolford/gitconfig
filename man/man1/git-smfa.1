.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.smfa(1)" "" "" "" ""
.hy
.SH NAME
.PP
git smfa
.SH SYNOPSIS
.PP
git smfa command - iterate over submodules and current directory to
execute shell command
.SH DESCRIPTION
.PP
git smfa is a helper alias to execute shell commands in the current
folder and in each submodule.
.SH EXAMPLES
.IP \[bu] 2
\f[C]git smfa pwd\f[R]
.IP \[bu] 2
\f[C]git smfa \[dq]touch .gitignore\[dq]\f[R]
.SH SEE ALSO
.PP
smf smg smga
.SH DETAILS
.IP
.nf
\f[C]
smfa = \[dq]!f() { : git man smfa; \[rs]
    [ $# -ne 1 ] && echo Usage: smfa command && return; : Ensure proper number of arguments; \[rs]
    $SHELL -c \[rs]\[dq]$\[at]\[rs]\[dq];                                   : Execute in container; \[rs]
    git smf \[rs]\[dq]$\[at]\[rs]\[dq];                                     : Iterate and ignore errors; \[rs]
    return 0;                                           : Return zero; \[rs]
}; f\[dq]
\f[R]
.fi
