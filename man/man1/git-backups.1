.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.backups(1)" "" "" "" ""
.hy
.SH NAME
.PP
git backups
.SH SYNOPSIS
.PP
git backups - list available backups from previous backup invocations
.SH DESCRIPTION
.PP
git backups is a helper alias to list available backups from previous
backup invocations.
.SH EXAMPLES
.IP \[bu] 2
\f[C]git backups\f[R]
.SH SEE ALSO
.PP
backup backuprm
.SH DETAILS
.IP
.nf
\f[C]
backups = \[dq]!f() { : git man backups; \[rs]
    cd \[rs]\[dq]$(git root)\[rs]\[dq];                          : Change directory to root; \[rs]
    bn=$(basename $(pwd));                       : Get name of folder for backup file names; \[rs]
    cd ..;                                       : Change directory up one folder; \[rs]
    if ls -ldhtr $bn.*.tar >/dev/null 2>&1; then : If a backup file exists; \[rs]
        ls -ldhtr $bn.*.tar;                     : Output backup file names; \[rs]
    else                                         : Else backup file does not exist; \[rs]
        echo No backups;                         : Output no backups; \[rs]
    fi;                                          : Close if statement; \[rs]
    return 0;                                    : Return zero; \[rs]
}; f\[dq]
\f[R]
.fi
