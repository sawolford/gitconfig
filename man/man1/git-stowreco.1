.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.stowreco(1)" "" "" "" ""
.hy
.SH NAME
.PP
git stowreco
.SH SYNOPSIS
.PP
git stowreco - stow working copy, re-checkout HEAD, then unstow
.SH DESCRIPTION
.PP
git stowreco is a helper alias to re-checkout HEAD.
.SH EXAMPLES
.IP \[bu] 2
\f[C]git stowreco\f[R]
.SH SEE ALSO
.PP
stow stowco unstow stowcob
.SH DETAILS
.IP
.nf
\f[C]
stowreco = \[dq]!f() { : git man stowreco; \[rs]
    echo $(git prheader) $(git emph stowco);            : Output command header; \[rs]
    if ! git stow; then return 1; fi;                   : Call stow alias to temporarily store Workspace; \[rs]
    cmd=\[rs]\[dq]reco\[rs]\[dq];                                       : Set next command; \[rs]
    echo $(git prinfo) re-checkout $(git prextra $cmd); : Output next command; \[rs]
    git $cmd;                                           : Check out target branch; \[rs]
    git unstow;                                         : Call unstow alias to restore Workspace to former condition; \[rs]
    return 0;                                           : Return zero; \[rs]
}; f\[dq]
\f[R]
.fi
