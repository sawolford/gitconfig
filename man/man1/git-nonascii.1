.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.nonascii(1)" "" "" "" ""
.hy
.SH NAME
.PP
git nonascii
.SH SYNOPSIS
.PP
git nonascii - print non-ascii lines in a grep-like way
.SH DESCRIPTION
.PP
git nonascii is a helper alias to print non-ascii lines in a grep-like
way.
.SH EXAMPLES
.IP \[bu] 2
\f[C]git nonascii file1 file2\f[R]
.SH SEE ALSO
.PP
nonasciiall notasciilist
.SH DETAILS
.IP
.nf
\f[C]
nonascii = \[dq]!f() { : git man nonascii; \[rs]
    if type pcregrep >/dev/null 2>&1; then command=pcregrep; : Use pcregrep command if available; \[rs]
    else command=\[rs]\[dq]grep -P\[rs]\[dq]; fi;                            : Else use grep -P; \[rs]
    $command --color=auto -n \[rs]\[dq][\[ha]\[rs]\[rs]x0-\[rs]\[rs]x7f]\[rs]\[dq] \[rs]\[dq]$\[at]\[rs]\[dq];       : Execute command to print non-ascii characters; \[rs]
}; f\[dq]
\f[R]
.fi
