.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.stowco(1) User manual" "" "April, 2020" "" ""
.hy
.SH NAME
.PP
git stowco
.SH SYNOPSIS
.PP
git stowco branch - stow working copy, checkout first argument, then
unstow
.SH DESCRIPTION
.PP
git stowco is a helper alias to checkout another branch.
.IP "1." 3
Ensures that the proper number of arguments have been provided
.IP "2." 3
Calls the stow alias to temporarily store the Workspace
.IP "3." 3
Checks out the target branch
.IP "4." 3
Calls the unstow alias to restore the Workspace to its former condition
.SH EXAMPLES
.IP \[bu] 2
git stowco $(git brupstream)
.IP \[bu] 2
git stowco master
.SH SEE ALSO
.PP
stowcob
.SH DETAILS
.IP
.nf
\f[C]
stowco = \[dq]!f() { : git man stowco; \[rs]
    [ $# -ne 1 ] && echo \[aq]Usage: stowco branch\[aq] && return 1; \[rs]
    if ! git stow; then return 1; fi; \[rs]
    git checkout $1; \[rs]
    git unstow; \[rs]
}; f\[dq]
\f[R]
.fi
.SH AUTHORS
Scott Wolford.
