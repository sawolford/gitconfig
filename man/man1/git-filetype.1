.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.filetype(1)" "" "" "" ""
.hy
.SH NAME
.PP
git filetype
.SH SYNOPSIS
.PP
git filetype - execute unix file command on all tracked files
.SH DESCRIPTION
.PP
git filetype is a helper alias to execute the unix file command on all
tracked files.
.SH EXAMPLES
.IP \[bu] 2
\f[C]git filetype\f[R]
.IP \[bu] 2
\f[C]git filetype -i ascii\f[R]
.SH SEE ALSO
.PP
mimetype notasciilist
.SH DETAILS
.IP
.nf
\f[C]
filetype = \[dq]!f() { : git man filetype; \[rs]
    command() { git ls-files -z | xargs -0 file; }; : Base command, file, to run; \[rs]
    if [ $# -gt 0 ]; then command | grep \[rs]\[dq]$\[at]\[rs]\[dq];    : If arguments, filter through grep; \[rs]
    else command; fi;                               : If not, just output; \[rs]
}; f\[dq]
\f[R]
.fi
