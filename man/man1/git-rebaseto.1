.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.rebaseto(1) User manual" "" "May, 2020" "" ""
.hy
.SH NAME
.PP
git rebaseto
.SH SYNOPSIS
.PP
git rebaseto branch - stow working copy, rebase HEAD onto first
argument, then unstow
.SH DESCRIPTION
.PP
git rebaseto is a helper alias to rebase HEAD onto another branch.
.IP "1." 3
Ensures that the current HEAD is not a detached HEAD
.IP "2." 3
Ensures that the proper number of arguments have been provided
.IP "3." 3
Ensures that the provided argument is associated with a branch
.IP "4." 3
Calls the stow alias to temporarily store the Workspace
.IP "5." 3
Calls the rebcombine alias to repeatedly merge changesets
.IP "6." 3
Calls the unstow alias to restore the Workspace to its former condition
.SH EXAMPLES
.IP \[bu] 2
git rebaseto $(git brupstream)
.IP \[bu] 2
git rebaseto master
.SH SEE ALSO
.PP
rebasefrom finishto mergeto
.SH DETAILS
.IP
.nf
\f[C]
rebaseto = \[dq]!f() { : git man rebaseto; \[rs]
    if ! git symbolic-ref HEAD; then echo \[aq]on detached head\[aq]; return 1; fi; \[rs]
    [ $# -ne 1 ] && echo \[aq]Usage: rebaseto branch\[aq] && return 1; \[rs]
    if ! git show-ref $1; then echo \[aq]branch not a named ref\[aq]; return 1; fi; \[rs]
    if ! git stow; then return 1; fi; \[rs]
    git rebcombine $1; \[rs]
    git unstow; \[rs]
}; f\[dq]
\f[R]
.fi
.SH AUTHORS
Scott Wolford.
