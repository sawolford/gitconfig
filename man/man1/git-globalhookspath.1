.\" Automatically generated by Pandoc 2.9.2.1
.\"
.TH "git alias.globalhookspath(1)" "" "" "" ""
.hy
.SH NAME
.PP
git globalhookspath
.SH SYNOPSIS
.PP
git globalhookspath - add core.hooksPath to \[ti]/gitconfig/local
.SH DESCRIPTION
.PP
git globalhookspath is a helper alias to add
core.hooksPath=$HOME/gitconfig/hooks to $HOME/gitconfig/local.
.SH EXAMPLES
.IP \[bu] 2
\f[C]git globalhookspath\f[R]
.SH DETAILS
.IP
.nf
\f[C]
globalhookspath = \[dq]!f() { : git man globalhookspath; \[rs]
    LOCALFILE=\[ti]/gitconfig/local;                                                 : Filename to write to; \[rs]
    grep \[aq]hooksPath=\[rs]\[dq]\[ti]/gitconfig/hooks\[rs]\[dq]\[aq] $LOCALFILE >/dev/null 2>&1 && return; : Ensure that config has not already been set; \[rs]
    echo \[aq]\[aq] >> $LOCALFILE;                                                       : Output a newline; \[rs]
    echo \[aq][core]\[aq] >> $LOCALFILE;                                                 : Output start of section; \[rs]
    echo \[aq]\[rs]thooksPath=\[rs]\[dq]\[ti]/gitconfig/hooks\[rs]\[dq]\[aq] >> $LOCALFILE;                      : Output hooksPath configuration; \[rs]
}; f\[dq]
\f[R]
.fi
